HTML Practice Repository

This repository contains my exercises and experiments while learning HTML fundamentals.
It includes practice with text formatting, structure, lists, links, images, semantic elements, and other core building blocks of web pages.

The goal is to create small, simple projects (like a blog page) to reinforce how each HTML element works.

📘 Summary of HTML Elements Used

Below is a quick reference of common HTML elements and their purposes.
Descriptions are intentionally short so I can easily review them in the future.

Basic Structure

<!DOCTYPE html> — Declares the document as HTML5.

<html> — Root element of the page.

<head> — Metadata container (title, links, charset).

<body> — Visible content of the page.

Text & Formatting

<h1>–<h6> — Headings (largest to smallest).

<p> — Paragraph of text.

<b> — Bold text (non-semantic).

<strong> — Important/strong text (semantic).

<i> — Italic text (non-semantic).

<em> — Emphasized text (semantic).

<br> — Line break.

<hr> — Horizontal divider.

Lists

<ul> — Unordered list (bullets).

<ol> — Ordered list (numbers).

<li> — List item.

Links & Media

<a> — Clickable hyperlink.

<img> — Displays an image.

<video> — Embeds a video.

<audio> — Embeds audio.

Containers & Layout

<div> — Generic block container.

<span> — Generic inline container.

<section> — Groups thematic content.

<article> — Self-contained piece of content.

<header> — Page or section header.

<footer> — Page or section footer.

<nav> — Navigation links container.

<main> — Main content area.

Tables

<table> — Table element.

<tr> — Table row.

<td> — Table data cell.

<th> — Table header cell.

<thead>, <tbody>, <tfoot> — Table grouping sections.

Forms

<form> — Collects user input.

<input> — Input field.

<label> — Label for an input.

<textarea> — Multi-line text box.

<button> — Clickable button.

<select> — Drop-down list.

<option> — Option inside a <select>.

Other Useful Elements

<meta> — Metadata (charset, viewport).

<link> — Links external files (CSS, icons).

<script> — Embeds or links JavaScript.

<title> — Page title (browser tab).

<!-- comment --> — Code comment.

🚀 Purpose of This Repository

Practice writing clean and valid HTML.

Build small static pages to strengthen fundamentals.

Create a personal reference for future web development learning.

Feel free to explore, copy, or expand any of the exercises!